# jishu-sdk-ios
Swift package for Jishu Promo access. Adds a simple import Jishu integration for iOS apps to expose a shareable app-scoped user ID,   check promo access through the Jishu API, and combine Jishu access with RevenueCat entitlements.
